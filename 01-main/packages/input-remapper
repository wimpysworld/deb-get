DEFVER=1
get_github_releases "sezanzeb/input-remapper" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="Input Remapper"
WEBSITE="https://github.com/sezanzeb/input-remapper"
SUMMARY="🎮 ⌨ :An easy to use tool to change the mapping of your input device buttons."

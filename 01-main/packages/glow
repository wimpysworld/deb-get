DEFVER=1
ARCHS_SUPPORTED="i386 amd64 arm64 armhf"
get_github_releases "charmbracelet/glow" "latest"
if [ "${ACTION}" != prettylist ]; then
    URL="$(grep "browser_download_url.*${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | head -n1 | cut -d '"' -f4)"
    VERSION_PUBLISHED="$(echo "${URL}" | cut -d '_' -f2)"
fi
EULA="MIT License"
PRETTY_NAME="Glow"
WEBSITE="https://github.com/charmbracelet/glow"
SUMMARY="Glow is a terminal based markdown reader designed from the ground up to bring out the beauty—and power—of the CLI."

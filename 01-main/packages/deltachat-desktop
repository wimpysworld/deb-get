DEFVER=1
ARCHS_SUPPORTED="amd64 arm64"
get_github_releases "deltachat/deltachat-desktop" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*${APP}_.*_${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="Delta Chat"
WEBSITE="https://delta.chat"
SUMMARY="Email-based decentralized messenger."

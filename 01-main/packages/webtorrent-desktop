DEFVER=1
get_github_releases "webtorrent/webtorrent-desktop" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL="$(grep "browser_download_url.*\.deb\"" "${CACHE_FILE}" | head -n1 | cut -d'"' -f4)"
    VERSION_PUBLISHED="$(echo "${URL}" | cut -d'/' -f8 | sed "s/v//")"
fi
PRETTY_NAME="webtorrent-desktop"
WEBSITE="https://github.com/webtorrent/webtorrent-desktop"
SUMMARY="Streaming torrent app for Mac, Windows, and Linux"

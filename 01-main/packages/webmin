DEFVER=1
get_website "https://sourceforge.net/p/webadmin/activity/feed.rss"
if [ "${ACTION}" != prettylist ]; then
    URL="$(unroll_url "$(sed 's|</*link>|\n|g' "${CACHE_FILE}" | grep -o -m 1 "^https.*_all.deb")")"
    VERSION_PUBLISHED="$(cut -d '_' -f 2 <<< "${URL}")"
fi
PRETTY_NAME="webmin"
WEBSITE="https://webmin.com/"
SUMMARY="Webmin is a web-based system administration tool for Unix-like servers, and services."

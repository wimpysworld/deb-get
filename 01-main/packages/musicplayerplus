DEFVER=1
ARCHS_SUPPORTED="amd64 arm64 armhf"
get_github_releases "doctorfree/MusicPlayerPlus" "latest"
if [ "${ACTION}" != prettylist ]; then
    URL="$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)"
    VERSION_PUBLISHED="$(sed -E 's|.*_([^.deb]*).*|\1|' <<< "{$URL}" )"
fi
PRETTY_NAME="MusicPlayerPlus"
WEBSITE="https://musicplayerplus.dev"
SUMMARY="Music server, player, and services management system."

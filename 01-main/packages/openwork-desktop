DEFVER=1
ARCHS_SUPPORTED="amd64 arm64"
get_github_releases "different-ai/openwork" "latest"
if [ "${ACTION}" != prettylist ]; then
    URL=$(grep -m 1 "browser_download_url.*linux-${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="OpenWork"
WEBSITE="https://openwork.software/"
SUMMARY="An open-source local-first team collaboration tool powered by OpenCode."

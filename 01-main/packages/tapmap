DEFVER=1
ARCHS_SUPPORTED="amd64"
get_github_releases "olalie/tapmap" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d'"' -f4)
    VERSION_PUBLISHED=$( cut -d'/' -f8 <<< "${URL//v/}")
fi
PRETTY_NAME="tapmap"
WEBSITE="https://github.com/olalie/tapmap"
SUMMARY="Watch your computer connect across the internet in real time. Discover the world behind your apps."

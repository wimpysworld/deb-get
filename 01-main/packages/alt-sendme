DEFVER=1
CODENAMES_SUPPORTED="bookworm trixie sid jammy noble questing"
get_github_releases "tonyantony300/alt-sendme" "latest"
if [ "${ACTION}" != prettylist ]; then
    URL="$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)"
    VERSION_PUBLISHED="$(cut -d '/' -f 8 <<< "${URL//v/}")"
fi
PRETTY_NAME="AltSendme"
WEBSITE="https://www.altsendme.com/"
SUMMARY="Send files and folders anywhere in the world without storing in cloud - any size, any format, no accounts, no restrictions."


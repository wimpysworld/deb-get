DEFVER=1
ARCHS_SUPPORTED=amd64
get_github_releases "AppFlowy-IO/AppFlowy" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="AppFlowy"
WEBSITE="https://appflowy.com/"
SUMMARY="AppFlowy is the AI collaborative workspace where you achieve more without losing control of your data. The leading open source Notion alternative."

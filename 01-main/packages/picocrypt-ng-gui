DEFVER=1
get_github_releases "Picocrypt-NG/Picocrypt-NG" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d'"' -f4)
    VERSION_PUBLISHED=$(cut -d'/' -f 8 <<< "${URL}" | tr -d v)
fi
PRETTY_NAME="Picocrypt NG"
WEBSITE="https://github.com/Picocrypt-NG/Picocrypt-NG/"
SUMMARY="A very small, very simple, yet very secure encryption tool."

DEFVER=1
ARCHS_SUPPORTED="amd64"
# some arm support but so far no arm deb release"
get_github_releases "fathah/hermes-desktop" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="Hermes Desktop"
WEBSITE="https://hermesone.org"
#WEBSITE="https://github.com/fathah/hermes-desktop"
SUMMARY="Desktop Companion for Hermes Agent"

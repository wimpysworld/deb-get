DEFVER=1
ARCHS_SUPPORTED="amd64 arm64 riscv64"
CODENAMES_SUPPORTED="trixie forky sid questing resolute"
get_website "https://obdev.at/products/littlesnitch-linux/download.html"
if [ "${ACTION}" != prettylist ]; then
    URL="https://obdev.at$(grep -m1 -o -P "^.*?href=\"\K[^\"]*${HOST_ARCH}\.deb(?=\")" "${CACHE_FILE}")"
    VERSION_PUBLISHED=$(cut -d '_' -f 2 <<< "${URL}")
fi
PRETTY_NAME="Little Snitch"
WEBSITE="https://obdev.at/products/littlesnitch-linux"
SUMMARY="Little Snitch for Linux monitors outgoing network connections and lets you decide which applications are allowed to connect to the internet."
